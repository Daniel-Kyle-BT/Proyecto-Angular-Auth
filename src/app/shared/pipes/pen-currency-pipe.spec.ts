import { PenCurrencyPipe } from './pen-currency-pipe';

describe('PenCurrencyPipe', () => {
  it('create an instance', () => {
    const pipe = new PenCurrencyPipe();
    expect(pipe).toBeTruthy();
  });
});
